name: test-checks
version: 1
description: Test schema with checks field
artifacts:
  - id: proposal
    generates: proposal.md
    description: Proposal document
    template: proposal.md
    instruction: Create a proposal document
    requires: []
    checks: [must-have-why]

  - id: design
    generates: design.md
    description: Design document
    template: design.md
    instruction: Create a design document
    requires: [proposal]
